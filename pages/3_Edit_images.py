import streamlit as st
from PIL import Image


# def show_page():
st.set_page_config(page_title='Edit Images', page_icon=':memo:', layout='wide')
st.title('🤖 :blue[A.I] Image Editor')
# uploaded_file = st.file_uploader("Upload an image", type=["jpg", "jpeg", "png"])
# if uploaded_file is not None:
#     col1, col2 = st.columns([1, 1])
#     with col1:
#         image = Image.open(uploaded_file)
#         st.image(image, caption="Uploaded Image")
#     with col2:
#         edit_prompt = st.text_input('',placeholder='Enter a prompt to edit the image...')

st.write("Edit image is not functional yet.")